from datetime import datetime

from math import factorial, log10

def catalan1(n):
    return factorial(2*n)//(factorial(n) * factorial(n+1))

def catalan1(n):
    for i in range(n)
    return factorial(2*n)//(factorial(n) * factorial(n+1))



n=100000


starttime=datetime.now()
print(log10(catalan1(n)))
#print(catalan(n))
print(format(datetime.now()-starttime))



